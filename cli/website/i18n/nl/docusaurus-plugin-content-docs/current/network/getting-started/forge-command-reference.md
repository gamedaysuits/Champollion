---
sidebar_position: 5
title: "forge Commandoreferentie"
description: "Elk nmt-forge subcommando, de bijbehorende argumenten en wat het bewaakt — gegenereerd vanuit de CLI-parser zodat het nooit veroudert."
---

<!-- GENERATED by forge/scripts/gen_command_reference.py — do not edit by hand.
     Re-run the generator after any CLI change. -->

# forge Opdrachtenoverzicht

Elk `nmt-forge` subcommando, rechtstreeks gegenereerd vanuit de CLI-parser zodat
deze pagina niet kan afwijken van het hulpmiddel. Voor de *reden* achter elke bewaker, zie
[Train een Model Eerlijk](/docs/network/getting-started/training-honestly); voor
het aansturen van forge vanuit een agent, zie
[Train Uw Eerste Model (met uw agent)](/docs/network/getting-started/train-your-first-model).

**De globale vlag:** `--workspace <dir>` (standaard `./.forge`) gaat vóór elk
subcommando en benoemt het project waarop u werkt.

Agents: roep `nmt-forge status --json` eerst aan — het vertelt u welke van deze u
vervolgens moet uitvoeren.


## `discover`

wat heeft deze taal? (leest de SSOT-taalkaart; afwezigheid = onbekend, nooit nul)

**Argumenten:**

- `code` — ISO 639-3-code (bijv. crk, fra, nav, arb)

**Opties:**

- `--cards-dir` — map voor taalkaarten (standaard: $CHAMPOLLION_CARDS_DIR of monorepo walk-up)
- `--json` — 
- `--no-registry` — sla de kruiscontrole van het mt-eval-register met evaluatiedatasets over

## `init`

stel een project op vanuit een taalkaart: werkruimte + startconfiguratie + NEXT_STEPS.md

**Argumenten:**

- `code` — ISO 639-3-code van de DOELTAAL

**Opties:**

- `--dir` — projectmap (standaard .)
- `--pair` — taalpaar als SRC-TGT (standaard eng-<code>)
- `--cards-dir` — 

## `status`

waar ben ik? toestandstabel + HET volgende commando (agents: roep dit eerst aan, gebruik --json)

**Opties:**

- `--json` — 

## `preflight`

zal <command> weigeren? elke poort die het zal raken, met oplossingen (exit 2 als een poort faalt)

**Argumenten:**

- `target` — commando om te preflighten: run|score|split|prereg|leak-audit

**Opties:**

- `--json` — 

## `lint`

diagnosticeer een batterijmanifest: zwakke registers → meest waarschijnlijke oorzaak → de hendel om als volgende aan te trekken

**Argumenten:**

- `manifest` — batterijmanifest json (bewaker: ci-scoring/battery)

**Opties:**

- `--run-manifest` — uitvoermanifest voor schema/transfer-plateau-signalen
- `--json` — 

## `registry`

eval-set-register

### `registry add`

**Argumenten:**

- `name` — 
- `path` — 

**Opties:**

- `--role` *(vereist)* (een van: dev, test, sealed) — 
- `--source-field` — 
- `--target-field` — 
- `--note` — 
- `--allow-rotate` — 

### `registry list`

### `registry add-harness`

**Argumenten:**

- `dataset_id` — 

**Opties:**

- `--role` (een van: dev, test, sealed) — 
- `--yes` — accepteer de ophaalverzoeken van de harness niet-interactief

## `split`

groepsdisjuncte train/dev/test-verdeling

**Argumenten:**

- `corpus` — 

**Opties:**

- `--test` *(vereist)* — 
- `--dev` — 
- `--seed` *(vereist)* — 
- `--out` *(vereist)* — 
- `--source-field` — 
- `--target-field` — 
- `--register` — registreer ook PREFIX-test / PREFIX-dev in de werkruimte

## `verify-split`

controle op nul-overlap van bestaande bestanden

**Argumenten:**

- `sides` — train/dev/test-bestanden (willekeurige deelverzameling)

**Opties:**

- `--source-field` — 
- `--target-field` — 

## `leak-audit`

screen een corpus ten opzichte van geregistreerde evaluaties

**Argumenten:**

- `corpus` — 

**Opties:**

- `--strict` — hard-fail bij test/sealed-treffers
- `--clean-to` — schrijf overgebleven rijen hier in plaats van te falen
- `--manifest` — 
- `--target-field` — 

## `sample`

per-soort begrensd reservoir-sample

**Argumenten:**

- `corpus` — 

**Opties:**

- `--n` *(vereist)* — 
- `--cap` — 
- `--key` — 
- `--seed` *(vereist)* — 
- `--out` *(vereist)* — 

## `ledger`

eval-grootboekinspectie

### `ledger show`

**Opties:**

- `--set` — bestedingsrapport voor één set

### `ledger verify`

## `prereg`

preregistratie

### `prereg new`

**Argumenten:**

- `id` — 

**Opties:**

- `--eval-set` *(vereist)* — 
- `--predictions` *(vereist)* — JSON-bestand: een lijst van predictieobjecten
- `--author` — 
- `--config-hash` — 
- `--consequences` — 

### `prereg check`

**Argumenten:**

- `id` — 

**Opties:**

- `--results` *(vereist)* — een ScoreReport-manifest JSON

## `score`

score hypothesen op een geregistreerde set (CI's altijd)

**Opties:**

- `--eval-set` — 
- `--config` — run-configuratiepad: stuurt de batterij aan vanuit het eval-blok (geregistreerde batterij, groepering, plugins, canonicalizer, prereg-binding) — één bestand, volledige evaluatie
- `--hyps` *(vereist)* — 
- `--config-hash` — 
- `--metric` — te scoren lane (herhaalbaar): chrf++, bleu, exact_match, comet, comet-qe, metricx (standaard: chrf++/bleu/exact_match)
- `--target-lang` — ISO 639-3-doelcode — bepaalt het juiste neurale metriekmodel en de bijbehorende waarschuwing voor lage-resourcetalen
- `--plugin` — LYSS-protocol metriek-plugin, 'module.path:ClassName' (herhaalbaar); de numerieke aggregaten worden CI'd lanes
- `--card-plugins` — voer de plugin-detectie van de harness uit voor deze taal (kaart evalMetrics + FST-geldigheid + gedragslinters)
- `--override-respend` — 
- `--json-out` — 

## `compare`

A/B op een geregistreerde set (prereg-beveiligd)

**Opties:**

- `--eval-set` *(vereist)* — 
- `--hyps-a` *(vereist)* — 
- `--hyps-b` *(vereist)* — 
- `--label-a` — 
- `--label-b` — 
- `--config-hash` — 
- `--metric` — te vergelijken lane (herhaalbaar; standaard chrf++) — incl. comet/comet-qe/metricx
- `--target-lang` — 
- `--plugin` — LYSS-protocol metriek-plugin (herhaalbaar)
- `--card-plugins` — harness plugin-detectie voor deze taal
- `--override-respend` — 

## `synth`

voer de synthese van een taalpakket uit

**Argumenten:**

- `pack` — een 'module.path:get_pack'-specificatie (bijv. nmt_forge_crk.pack:get_pack) of de entry-point-naam van een geïnstalleerd pakket (bijv. crk)

**Opties:**

- `--out` *(vereist)* — 
- `--seed` — 
- `--limit` — 

## `run`

reproduceerbare trainingsrun met één commando

**Argumenten:**

- `config` — 

## `evaluate`

sluit de lus: decodeer de batterij van de configuratie met het GESELECTEERDE checkpoint van de run, score het (CI's, prereg-beveiligd) en diagnosticeer automatisch — geen handmatige decodering

**Argumenten:**

- `run_manifest` — run-manifest.json geschreven door `nmt-forge run`

**Opties:**

- `--config` — run-configuratiepad (standaard de configuratie ingebed in het run-manifest); moet een eval-blok bevatten
- `--out-hyps` — waar gedecodeerde batterijhypothesen worden geschreven (standaard: naast het run-manifest)

## `report`

genereer het rapport in gewone taal opnieuw vanuit een run- of batterijmanifest

**Argumenten:**

- `manifest` —
