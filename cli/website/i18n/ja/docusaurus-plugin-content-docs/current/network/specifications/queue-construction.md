---
sidebar_position: 8
title: "キュー構築仕様"
slug: '/network/specifications/queue-construction'
description: "コミュニティコンピュートキューの透明な計算式：期待チェーン値によるランキング、すべての構成要素を公開し、すべての順位を手動で再現可能。"
related:
  - label: "Why the Queue Is Built This Way"
    to: /docs/network/perspectives/why-the-queue
    kind: position
    note: "The philosophy behind this formula"
  - label: "Contributing Compute"
    to: /docs/network/getting-started/contributing-compute
    kind: guide
    note: "How to actually run queue items"
  - label: "Scoring Specification"
    to: /docs/network/specifications/scoring
    kind: spec
  - label: "Corpus Design Framework"
    to: /docs/network/specifications/corpus-design
    kind: spec
    note: "Small-corpus floors and noise thresholds the formula inherits"
---

# キュー構築仕様

**数式バージョン: `ecv-v3`（ブリッジ信頼性を考慮した期待チェーン値）。** このドキュメントは、[champollion.dev/queue.json](https://champollion.dev/queue.json) の順序付けに関する規範的な定義です。実装（公開ハーネスリポジトリ内の `arena/scripts/generate_sweep_queue.py`）はこのページをセクションごとに反映しており、キューのメタデータは生成時に使用された正確なパラメータ値を反映しています。また、**すべてのアイテムには数式の完全な内訳が含まれている**ため、公開されている JSON だけから手作業で任意のランクを再導出できます。このページとキューの内容が一致しない場合はバグです — ご報告ください。

**現在のキューの概要（1段落で）。** パブリックキューは、LLMアイテム（ナイーブおよびコーチングプロンプト条件）とMTサービスエンジンアイテムの両方を1つのボードに保持し、サーベイ順序（`map`、§2.2）によってランク付けされます。具体的には、1ドルあたりのペア、言語、語族全体での初回の測定（first light）、これまで測定されたことのない言語に対する初回測定ブースト（§2.2）、プレビューで公開される予算階層（§2.1.1）、およびデータベースから提供される完全なランキング（完全なランキングがサイズ上限を超えた場合、静的ファイルは上位スライスを保持し、その旨を記載します）を含みます。以下のセクションは規範的な定義であり、日付付きの決定履歴とともに保持されています。提供されるキューのメタデータには、それをランク付けした正確なパラメータが指定されています。

> **v3 (2026-06-13).** すべてのエッジは、品質と信頼性という 2 つの数値を持つ*ブリッジ*となり、チェーン行列はその積で動作します（§1.5）。62 件の単語レベルの語彙アイテムを 1 回実行しただけでは、もはやパスとは見なされません。レプリケーション、より大きなコーパス、より豊富なコーパス、より狭い信頼区間はすべて価格付きの価値を持ちます。v2 キュー（品質のみ）は、それぞれのメタデータを通じて引き続き解釈可能です。

## 1. 目標：品質加重メッシュ

このプロジェクトの使命は、*測定された個別ペアチェーンによって、すべての言語をすべての言語へ*翻訳することです。直接のベンチマークが存在しない 2 言語間の翻訳は、ベンチマーク済みのペアを**チェーン**することで提供されます（X→ピボット→Y）。そのため、ベンチマークの価値はコーパスの数ではなく、そのグラフの**チェーン容量**にあります。

**定義。** *ベンチマークグラフ*は言語ごとに 1 つのノードを持ち、少なくとも 1 件の公開済みかつ失格でない実行が存在するすべての言語ペアに対して、**エッジ強度**

```
s(e) = (best published corpus-level chrF++ on that pair) / 100   ∈ [0, 1]
```

コーパスレベルの chrF++ が正規の公開値です（[スコアリング仕様](/docs/network/specifications/scoring)を参照）。*best* を使用するのは、チェーンがホップごとに最良の実証済みシステムを経由するためです。公開済み実行が存在しないペアは s(e) = 0 となります。

2 言語間のパス P の**推定チェーン強度**は

```
strength(P) = λ^(|P|−1) · Π_{e ∈ P} s(e)
```

— エッジの品質は乗算的に合成され、各*ジャンクション*（各中間ピボット）には追加の忠実度係数 **λ < 1** のコストがかかります。どちらの選択もピボット翻訳の文献に基づいています。ピボットを経由した翻訳は、単純な合成が示す以上に、直接翻訳と比べて品質が低下することが知られており（Utiyama & Isahara 2007; Wu & Wang 2007）、その損失の大きさはピボットの選択に依存します（Paul et al. 2009）。また、英語中心でない*直接*ペアを構築することは、M2M-100 の多対多設定において英語ピボットを大規模に上回ることが実証されています — 約 10 BLEU の差（Fan et al. 2021）。λ は、推定チェーンは測定値ではないという数式上の継続的な注意喚起です。直接実行のみがこの割引を取り除きます。

**最良チェーン行列**と**メッシュ目標**は次のとおりです。

```
Q(u,v) = max over paths P from u to v of strength(P)      (1 if u = v, 0 if disconnected)

Φ = mean over ordered language pairs (u ≠ v) of Q(u,v)    ∈ [0, 1]
```

Q は標準的な対数変換（エッジ重み −ln(λ·s(e)) ≥ 0、Dijkstra 法、その後 Q = exp(−d)/λ）による最短経路問題として正確に計算されます。Φ は [Latora & Marchiori (2001)](https://arxiv.org/abs/cond-mat/0101396) の*グローバル効率*の構成であり、1/距離カーネルを乗算的チェーン忠実度に置き換えたものです — エッジがユニット長ではなくホップごとの品質保持を持つ場合の自然なカーネルです。（キュー v1 は非加重グローバル効率ゲインでランク付けしていました — エッジの存在のみが既知である場合のこのファミリーの特殊ケースです。）

### 1.5 信頼性：ブリッジは (q, r) である

小さく薄く、一度も再現されていないコーパスでの派手なスコアはブリッジではありません。そのため v3 では、測定済みのすべてのエッジを次のように分割します。

```
quality      q(e)   = best published corpus-level chrF++ / 100
reliability  r(e)   = f_size · f_rich · f_conf · f_repl        ∈ [0, 1]
effective    s_eff(e) = q(e) · r(e)        ← what chains compose over
```

| 係数 | 定義 | 満点となる条件 | 根拠 |
|---|---|---|---|
| `f_size` | min(1, n/100)、n = 最良実行の評価エントリ数 | 100 エントリ | [コーパス設計](/docs/network/specifications/corpus-design)の有意性フロア。Koehn (2004) は約 300 文のセットでブートストラップ検定を検証しています — 300 でも「小さい」ため、サイズは表示のゲートではなく信頼性を割り引きます |
| `f_rich` | min(1, L̄/5)、L̄ = 平均*有効*ソース長 | 有効語 5 語 | AmericasNLP（Mager et al. 2021）は、語レベルの単位が豊かな形態論で機能しないため chrF を採用しました。Mager et al. (2022) は空白トークンが誤った単位であることを記録しています |
| `f_conf` | min(1, 5/h)、h = 最良実行の chrF 95% CI 半幅（未公開の場合はプロキシ `50/√n`） | CI ≤ ±5 chrF | 小さなコーパスではデルタが区別できないノイズフロア。Kocmi et al. (2021) は CI 内のデルタが人間の好みと頻繁に矛盾することを示しています |
| `f_repl` | min(1, runs/2) | 公開済み実行 2 件 | Marie, Fujita & Rubino (2021) は 769 本の論文をメタ評価し、再現されていない単一比較がこの分野の記録された信頼性の失敗であることを示しています |

**有効長**は空白区切りの語数ではなく、コンテンツ単位で測定されます: `L̄ = mean source chars / c(L)`。ここで*文字経済性* `c(L)` は、アラインされた側の英語 1 語に対する言語 L 側の中央文字数であり、このプロジェクト自身の並列コーパスから測定されます（v3 リリース時点で 7,400 件以上のアラインエントリ: cmn 1.6、jpn 2.3、kor 2.6; eng ベースライン 5.0; deu 6.0; crk 4.7 — 多合成語はその内容に応じて価格付け）。類型論のルックアップテーブルは使用しません。コーパスが成長するにつれて推定値は精緻化されます。eng とのペアデータがない言語はデフォルトの経済性を使用します。レジストリの `richness` ブロックにコーパスごとに記録されます。

**ブリッジ階層**（表示用語）: **established** — n ≥ 100、L̄ ≥ 5、h ≤ 5、runs ≥ 2; **provisional** — 測定済みだがいずれかの条件を満たさない; **registered** — 公開済み実行なし。チェーンの主張（「X から Y へ到達できる」）は、最も弱いホップの階層と同程度の強さしかなく、メッシュの可視化では信頼性をエッジの不透明度で表示します。

**検証例**（v3 リリース前に実行された検証スクリプトより）: *62 件の単語レベル語彙アイテム、1 回の実行* → r ≈ **0.04**（パスではない）; *200 文、±3 CI、3 回の実行* → r = **1.00**; 素朴な語数が 1.0（スクリプトの成果物）の 101 エントリの日本語コーパスは、6.5 有効語に修正され、`f_rich` が満点となります。境界値とファクターごとの単調性はプロパティテストで検証されています。

**v3 における実行の価値。** 実行はブリッジを 2 つの方法で改善でき、ΔΦ はそのうち良い方を採用します: **(a)** エッジの最良実行となる — `ŝ_eff = 予測品質 × r(そのコーパスの n、豊富さ、CI プロキシ、runs+1)`; または **(b)** 単に再現する — 現在の最良値は変わらず、`f_repl` が上昇します。したがって、単一実行エッジへのレプリケーションは実際の価格付きの価値であり、測定済みペアに対してより大きいまたは豊富なコーパスを使用することは、小さいコーパスの再実行よりも優先されます。アイテムは `edge_quality`、`edge_reliability`、`edge_tier`、`effective_strength`、`post_run_reliability`、`predicted_effective` を v2 の予測フィールドとともに公開します。

**Φ でないもの。** Φ はキューの内部優先順位付けの通貨であり、能力の主張ではありません。その入力は[コーパス設計フレームワーク](/docs/network/specifications/corpus-design)のすべての注意事項を持つ開発セットスコアです。訓練データの汚染の可能性により各スコアは上限値となり、chrF++ の値は言語間で厳密には比較できず、小さなコーパスは広い信頼区間を持ちます。数式は Φ を*有用性による実行の順序付け*にのみ使用します。品質保証として公開されることはありません。

## 2. 決定問題

キューのオープンアイテムは、適格であり（開発スプリット、再配布可能なライセンス、隔離されていない、送信適格、および**ベンチマーク解決可能** — §2.2の言語アイデンティティゲートを参照）、まだリーダーボードにないすべての（コーパス、モデル、条件）の組み合わせです。カバーされた組み合わせの同一の再実行は除外されます（公開時にランカードのフィンガープリントによって重複排除されます）が、すでに測定されたペアの新しいモデルや条件はオープンアイテムとして残ります。

提供されたコンピュートは予算です。メッシュが最も速く改善されるように次に実行するオープンアイテムを選択することは、予算付きのカバレッジスタイルの最大化問題です。正規のアプローチは**単位コストあたりの限界価値**によるグリーディ選択です。単調劣モジュラ目標に対して、グリーディルールは古典的な (1 − 1/e) 保証を持ち（Nemhauser, Wolsey & Fisher 1978）、その利益/コスト比の形式は予算制約下での標準アルゴリズムです（Khuller, Moss & Naor 1999）。私たちはこの比率ルールをランキング原則として使用します。（正直な注記: 私たちの目標は決定論的なコアに被覆的な収穫逓減を持ちますが、確率的予測層があるため、グリーディ保証はこの正確なシステムに関する定理としてではなく、*動機付け*として引用しています。）

```
ECV(item) = ΔΦ(item) / max(est_cost_usd, COST_FLOOR)
```

アイテムは ECV の降順でランク付けされます。同点の場合: naive が coached より優先、コストが低い方が優先、次にアイテム ID で決定します。

### 2.1 ランキングの修正 — 2026-07-12

貪欲 ECV ルールに重ねて適用される4つの調整があり、それぞれキューのメタデータ（`priority_parameters.contamination_ecv_factors`、
`priority_parameters.frontier_interleave`、`metadata.preview_policy`）に反映されます。

1. **汚染乗数。** 各アイテムの ECV に、そのコーパスの汚染グレードに基づく係数を乗算します：**LOW 1.0 / MEDIUM 0.4 / HIGH 0.1**。グレードが不明または欠落している場合は MEDIUM として扱います（クリーンであると仮定しないこと）。理由：クリーンチェーングラフは LOW 汚染のエッジのみを受け入れるため、非 LOW の実行はそこに入ることができず、同等のコストでクリーンメッシュの作業より上位にランクされてはなりません。非 LOW のアイテムはキューに残ります — レーン内の相対比較は実際の価値を持ちます — ただし、クリーンな作業の後ろにランクされます。
2. **フロンティアのインターリーブ。** 貪欲ソートの後、5番目ごとの優先スロットに、フロンティアモデルセット（ジェネレーターのデータとして管理され、メタデータに反映される）からまだ配置されていない最上位のアイテムが割り当てられます。これにより、安価な層が飽和した後ではなく、早い段階でフロンティアの証拠が予測事前分布に届きます。純粋な並び替えであり、アイテムの削除や重複はありません。自然なスロットを獲得したフロンティアアイテムはそのスロットを維持し、優先度は織り込まれた順序から番号付けされます — 公開されたランキングが真の順序です。
3. **プレビューのソースハブ上限。** 上位25件のパブリックプレビューでは、同一ソース言語を共有するアイテムは最大 **6件** まで表示されます。これにより、リソースが豊富な単一のハブがショーウィンドウを独占できないようにします。上限を超えたアイテムは、フルキュー内での実際の優先度を維持します。プレビューは単にランキング順で次の適格なアイテムを取得します。
4. **プレビューの人工言語除外。** ソースまたはターゲットが人工言語であるアイテムは、プレビューでスキップされます。判定はカードファミリー駆動（Glottolog の Artificial Language バケット、言語カードから読み取り — ハードコードされた言語セットは使用しない）で行われ、導出されたコードリストは `metadata.preview_policy` に公開されるため、サーバーサイドの更新でも同じ選択が適用されます。

(3) と (4) は**表示ポリシーのみ**です：フル `queue.json`、そのランキング、および優先度には影響しません。

### 2.1.1 予算階層 — 「Xドルで何が買えるか？」（2026-08-24）

`queue-preview.json` は `budget_tiers` 配列を保持し、**$1 / $10 / $100 / $1000** の予算について、公開されたランキングの貪欲な手頃なプレフィックス（greedy affordable prefix）を要約します。優先順位に従ってアイテムをたどり、見積もりコストがまだ予算内に収まる各アイテムを取得し、収まらないものはスキップし、後続のより安価なアイテムで埋め続けます。各階層は、購入できるアイテム数、その合計見積もりコスト、関連する個別の言語ペアとモデルの数、および予算がランキングのどの深さまで到達するか（`max_priority`）を報告します。

ランキングはすでにコストあたりの限界価値（§2）に基づいているため、貪欲な手頃なプレフィックス（greedy affordable prefix）は、このモデルがその支出に対して推奨する割り当てそのもの**です**。小規模なコントリビューターも大規模なコントリビューターも、誰にも合わない暗黙のサイズの1つのリストではなく、同じ公開されたランキングから具体的で最適な回答を読み取ります。階層は単なる要約です。割り当て自体は、自身の予算に対して順番にたどったランキングにすぎません。サーバー側の更新では、残存するアイテムに対して同じ手順で階層を再計算します（ジェネレーターと更新関数は双子として実装され、両側でテストされています）。

### 2.2 レーンとランキングモード — 2026-07-19

提供されるキューは、自身のメタデータ内で、どの**レーン**を保持し、どの**ランキングモード**で順序付けされたかを宣言します。メタデータが信頼できる情報源であり、このセクションではその語彙を定義します。

**レーン** (`metadata.lane`, `metadata.lane_policy`)。2026-08-27以降、パブリックキューは **both** レーンを保持しています。つまり、LLMアイテム（モデル × プロンプト条件）**および** MTサービスアイテム（条件 `engine` — DeepL、Google Translate、Microsoft Translator、LibreTranslate、Tilde。それぞれが公開しているカバレッジリスト内のペアに対してのみキューに入ります）です。2026-07-19の **llm** レーン（LLMアイテムのみ。少なくとも一方がすべてのMTサービスの公開カバレッジ外であるペアに限定）は、実行されなかった主催者主導のキャンペーンのためにサービスベンチマークを予約しており、カタログの大部分を保留状態にしていました。サービスを測定すること*こそ*がカバレッジマップのバックボーンであるため、現在では両方の種類の作業が1つのボードに配置されています。カバレッジの和集合（言語カードを介してマクロ言語のエイリアスが設定されたもの）は引き続き `service_coverage_methods` および `service_covered_languages` としてエコーされ、llmレーンのキューは引き続き除外されたペアを `pairs_dropped_fully_covered` として報告します。

**Blobサイズ上限** (2026-08-27)。提供される `queue.json` は、ホスティングの厳格な上限を持つ静的ファイルであるため、完全なランキングがその上限を超えた場合、ファイルはランキングの**上位スライス**を保持し、`metadata.blob_truncated {kept, total}` にその旨を記載します。暗黙のうちに上限が適用されることはありません。データベースキュー (`queue_top()` / `queue_pairs()`) は常に**完全な**ランキングを提供し、信頼できる作業リストとなります。プレビューのペア集計と予算階層は、それらが同梱される成果物について説明します。

**言語アイデンティティゲート** (2026-07-19)。キューのアイテムは、**アクティブな個別のISO 639-3コード**のみを対象とします。マクロ言語（「アラビア語」）や集合的な語族コード（「ベルベル諸語」）に対するスコアは、評価されたことのない変種に関する反証不可能な主張になってしまうためです（これは、FLORES-200/NLLBがデータを `arb`/`quy`/`zsm` としてコーディングするのと同じ理由です）。アップストリームのコーパスラベルは*解決*され、そのまま従ったり破棄されたりすることはありません。文字（スクリプト）タグは機械的に削除され（`eng→cmn-Hans` コーパスは `eng→cmn` としてキューに入り、文字はアイテムの表示メタデータ `source_script`/ `target_script` として保持されます）、完全に廃止されたコードは公式のISO後継コードに従います。また、マクロラベルが付けられたコーパスは、レジストリエントリに記録され引用された**変種解決（variety resolution）**の下でのみキューに入ります（例：FLORES+はそのケチュア語を `quy` として文書化しています）。どちらのパスでも解決されないコーパスは除外され、機械可読な理由が `metadata.doctrine_exclusions` （合計、理由別の数、コーパス別の理由）で公開され、デザート台帳 (`service_landscape.invisible_reasons.corpus_excluded_by_doctrine`) にカウントされます。これらは可視化された除外であり、暗黙のうちに削除されることはありません。包括的なラベルが付けられたコーパスの過去の結果は、誠実に命名された独自のメッシュノード（ノード `scope`: `macrolanguage` / `collective` / `retired`）を保持し、メンバーの変種に統合されることは決してありません。解決の入力はすべて公開されています。レジストリのエントリごとの `language_resolution` スタンプには、解決されたコード、スコープ、およびピン留めされた引用が含まれています。

**ランキングモード** (`metadata.rank_mode`、`metadata.priority_model` で説明)。同じアイテムに対する2つの順序付け：

- **ecv** — §2–§3の貪欲な期待チェーン値（expected-chain-value）ルール：見積もりドルあたりのメッシュの改善。活用（exploitation）の順序付けであり、ボードが予測に十分な密度を持ち、ΔΦがシグナルを伝達できる場合に適しています。
- **map** (map-value v2) — サーベイ順序付け：`MapValue = novelty × uncertainty × promise × connectivity × corpus-quality × contamination ÷ cost` であり、厳密な貪欲トレースによって組み立てられます。*Novelty（新規性）*は、位置的な初回の測定（first-light）クレジットであり、すでに配置されたアイテムが同じ有向ペア (1/(1+n))、ターゲット言語、ターゲット語族、メソッド × ターゲット語族セル、およびターゲット × ドメインセル（それぞれ 1/√(1+n)。語族は言語カードから、ドメインはコーパスレジストリの分類法から取得されます。ターゲットの初期カバレッジは、最初に測定されたドメインを繰り返すのではなく、レジスター全体に広がるべきです）を占有するにつれて減衰します。*Uncertainty（不確実性）*は、§3.1の予測のバックオフ深度（ペア 0.25 · ターゲット言語 0.55 · ソース言語 0.75 · グローバル 1.0）× 1/(1+エッジ上の公開された実行数) です。*Promise（見込み）*は、§3.1の予測強度を0.25で下限としたものです。機能しそうな未知のものが先行し、可能性のあるデザートをマッピングすることにも価値があります。*Connectivity（接続性）*は、**測定されたネットワークをまだ到達できない言語にリンクする**ペアを上位にランク付けします。エンドポイントは、測定されたメッシュエッジ (`mesh.json`、ステータス `measured`) 上にあるか、いずれかのMTサービスの公開カバレッジリスト（マクロ言語のエイリアスが設定されたもの。上記のレーンゲートと同じエイリアス）内にある場合に*確立された（established）*と見なされます。**ブリッジ**（確立されたエンドポイントが正確に1つ）と**島（islands）**（どちらも確立されていない）はどちらも1.0のスコアになります。2026-08-27以降、切断されたデザートの初回の測定は完全にカウントされます（2026-07-19のネットワークから成長するサイジングでは島は0.5のスコアであり、構造的に最も深いテールを降格させていました）。一方、**内部（interior）**の高密度化（両方が確立されている）は0.5のスコアになります。既知のポイント間の強化はecvモードの役割です。**初回測定ブースト（first-reading boost）** (×2.0) はさらに、ソース言語またはターゲット言語の公開された測定値がどこにもゼロであるアイテムのサーベイ値に乗算されます。第9の原則を明確に述べると、**言語の初回測定は改良よりも優先される**ということです。不確実性要因だけではこれを表現できません（2つのよく測定された言語間の未測定ペアを、一度も測定されたことのない言語と同じようにスコアリングします）。このブーストにより、ロングテールの初回の測定は、偶発的な結果ではなく、明示的な目標になります。両方の要因は `metadata.map_value_parameters` に乗り、edvのサーベイコンポーネント（§2.3）内で同様に適用されます。

  第9の原則のもう半分は、ランキングの**外側**に存在します。既存のアイテムをどのように順序付けても、コーパスがまったくない言語（現在、約7,500の生きた個別コード言語）には到達できません。**コーパスウィッシュリスト** (`/corpus-wishlist.json`、キューの傍らで再生成される) は、その獲得のフロンティアを公開します。コーパスがゼロのすべての生きた個別コード言語が、引用された最高の話者数によってランク付けされます。話者数は、実際にコーパスを構築できるコミュニティの実現可能性の代用指標として機能し、すべての数はそのソースに帰属し、決して調停されません。
  *Corpus-quality（コーパスの品質）*は、§1.5のコーパスの固有の信頼性の可能性 `f_size × f_rich` です。サーベイは重みに耐えられるコーパスに着地すべきであるため、62エントリの単語語彙リストが単に安価であるという理由だけで見出しを飾ることはなくなりました。欠落しているリッチネス測定値は中立のままです（測定値がないことは貧弱さの証拠ではありません）。コストと汚染の規律はecvと同じです。フロンティアのインターリーブとタイブレーク（§2.1）は変更なく適用されます。サーベイフェーズに適しています。これは、1ドルあたりに*マップが学習する*内容を最大化します。つまり、ペア、言語、語族、メソッドセル、およびドメイン全体での最初の測定であり、分散するのではなく測定されたネットワークから成長します。これは、メッシュ強度の成長が遅くなるという意図的な代償を払っています。

> **map-value v2 (2026-07-19)。** サーベイ順序付けに対する創設者主導の2つの追加事項：*測定されたネットワークにブリッジする*ペアが、切断されたプローブや内部の高密度化よりも上位にランク付けされるようになりました。また、コーパスの品質（サイズの下限 × 有効なリッチネス、§1.5）とターゲットごとのドメインの広がりがランキングに重み付けされます。コントリビューターの計算リソースは、重みに耐えられる十分な品質のコーパス上で、確立されたパスを新しいパスにリンクする必要があります。ライセンスは引き続き**ゲートであり、重みではありません**。ライセンスと送信チャネルのルールは、そもそも何をキューに入れることができるかを決定します（§2、およびキューの `transmission_note`）。適格なコーパスの中では、ランキングはライセンスを考慮しない（license-blind）ため、制限されているがピン留めされた研究セット（多くの場合、ペアの唯一のコーパス）が体系的に枯渇することは決してありません。v1キュー（新規性 × 不確実性 × 見込みのみ）は、独自のメタデータを介して引き続き解釈可能です。

生成時に使用された正確な係数値は `metadata.map_value_parameters` に同梱されます。接続性と品質の入力は、公開されている `mesh.json` （測定されたエッジ）、メタデータにエコーされたサービスカバレッジの和集合、および `registry.json` （エントリ数 + リッチネス）から再導出可能です。さらに、すべてのアイテムはモードに関係なく完全なecv-v3診断フィールドを保持しているため、どちらの順序付けも同じ成果物から再導出できます。

### 2.3 ランキングモード `edv` — 期待決定値 (expected decision value) (2026-08-27)

*ステータス：実装済み。§2.3.6での測定された比較を待つ間、デフォルトではオフになっています。それまで、公開されるデフォルトは `map` のままです。*

キューは正確に2つの製品を購入します。**機能マップ（capability map）**（どのメソッドが何に優れているか、誠実な不確実性を伴う）と**ルーティングメッシュ（routing mesh）**（ルートに連なる測定されたペア）です。`edv` は、加重ポートフォリオとして、各候補アイテムが両方をどれだけ前進させるかによって価格を付けます。

```
EDV(item) = [ w_judge·Ĵ + w_mesh·M̂ + w_survey·Ŝ ] × contamination ÷ max(cost, COST_FLOOR)
```

デフォルトは `w_judge = 0.35, w_mesh = 0.25, w_survey = 0.40` です（創設者が調整可能。すべての生成において、実際に使用された重みが `metadata.edv_parameters` にエコーされます）。汚染係数（§2.1 解決策1）は、外部の乗数として正確に1回適用されます。ライセンスと送信は引き続き**ゲートであり、重みではありません**。適格性は値が計算される前に決定され、適格なコーパスの中ではランキングはライセンスを考慮しません（license-blind）。

#### 2.3.1 Ĵ — メソッド判定値 (method-judgment value)

実行が**同一コーパスのメソッド比較の決着**をどれだけ前進させるかを価格付けします。これは、このプロジェクト独自の測定研究が認可する唯一のクロスメソッドの主張です。（W2の難易度転移研究は、言語間の能力のリンクを拒否しました。その認可された肯定的な結果である、言語内の加法的なメソッド × コーパスの調整こそが、このコンポーネントが使用するものです。キャリブレーションパイロットに従い、スコアは順序付けと分離にのみ使用され、許容確率に変換されることは決してありません。）

候補（コーパスC、メソッドM、条件）について：**対比パートナー（contrast partners）**は、（C、同じ条件）ですでに公開された実行を持つメソッドM′です。各パートナーについて、`sep` をプールされたCI半値幅（記録されたCI。未公開の場合はプロキシ `50/√n`）に対するchrFポイントでのスコア分離とし、`sep_pred` を§3.1の予測スコアに対して計算された同じものとします。

| ペア上の {M, M′} の対比状態 | クレジット |
|---|---|
| **未遭遇 (unmet)** — 共有コーパスがまだない | `JUDGE_FIRST = 1.0` |
| **競合中 (contested)** — 共有コーパスが存在し、すべて `sep < Z_DEC` | `JUDGE_CONTESTED = 0.8 × clip(sep_pred / Z_DEC, 0, 1)` |
| **決着済み (decided)** — 一部が `sep ≥ Z_DEC`、n_dec 個のコーパスがそれを決定する | `JUDGE_DECIDED = 0.25 / (1 + n_dec)` |

それぞれに `w_top = 1/√(rank(M)·rank(M′))` が乗算されます。1位と2位の決着をつけることは、7位と8位の決着をつけることよりも価値があります。ペアごとのメソッドランキングは、ペアに2つ以上のメソッド × 2つ以上のコーパスが測定されている場合、認可された加法的なメソッド × コーパスの適合（観測されたセルに対する交互最小二乗法）を使用し、そうでない場合はメソッドごとのベストスコアを使用します。この適合は**厳密にペアごとであり、言語間でプールされることは決してありません**。`Z_DEC = 1.96`。

同じ (C, M) でのコーチング対ナイーブの対比は `JUDGE_COND = 0.5 / (1 + n_cond)` を追加します。アイテムの対比は、収穫逓減（追加の対比ごとに `JUDGE_GAMMA = 0.7`、降順でソート）で合計され、さらに**シード項** `JUDGE_SEED = 0.25 × min(1, m_C/3) × corpus-quality` （m_C = C上にキューアイテムを持つ他のラインナップメソッド）が加算されます。これにより、空のボードでも将来の比較を判定できるコーパスが優先されます。これは場（venue）の価値であり、借り物のスコアではありません。組み立て中、判定コンポーネントは `1/(1 + items already placed on the same pair and condition lane)` 減衰します。

#### 2.3.2 M̂ と Ŝ

`M̂` は§3の期待メッシュゲイン (ΔΦ) であり、変更はなく、チェーン行列は生成時に固定されます。`Ŝ` は§2.2のmap-value v2のコアであり、位置的な新規性の減衰を伴う `uncertainty × promise × connectivity × corpus-quality` であり、変更はありません。予測スコアの*レベル*（見込み）は Ŝ にのみ存在します。Ĵ はスコアの*分離*のみを使用します。2つのコンポーネントが同じ楽観的観測を二重にカウントすることはありません。

#### 2.3.3 正規化

3つのコンポーネントは比較不可能なスケール上に存在するため、各静的コンポーネントは候補セット全体の95パーセンタイルで除算されます（上限は `EDV_NORM_CAP = 4.0`）。3つの正規化係数は `metadata.edv_parameters.normalizers` に同梱されており、公開されたすべてのEDV値が独自の成果物から再導出可能になっています。

#### 2.3.4 組み立て

順序付けは、mapモードとまったく同じ遅延貪欲（lazy-greedy）トレースです。順序に依存するすべての乗数（サーベイの新規性、判定配置の減衰）は、アイテムが配置されるにつれて単調非増加となるため、古いヒープエントリは過大評価することしかできません。つまり、遅延貪欲の不変条件が成り立ち、トレースはブルートフォースの貪欲法と等しくなります。フロンティアのインターリーブ、プレビューポリシー、および予算階層は変更なく適用されます。

#### 2.3.5 説明可能性

すべてのアイテムは、その診断情報に以下を保持します。クレジットされた対比リスト（パートナー、状態、予測される分離、ランクの重み）、シード項と減衰項、すべての§2.2および§3のフィールド、重みと正規化係数。公開されたEDV値は、その行から正確に再計算可能です。「このアイテムはどのようにしてこのランクになったのか？」という疑問には、外部の状態なしで答えることができます。

#### 2.3.6 採用基準

`edv` は、同じボード上での `map` および `ecv` との測定された比較の後にのみ、公開されるデフォルトになります。すべてのサーベイ指標（初回の測定の深度パーセンタイル、深度における個別のペア/言語/語族、限界新規ペア率）でmapの10%以内であり、両方の判定指標（シミュレートされた1,000ドルあたりに解決された競合する対比、固定支出でのメソッドランキングの回復）で厳密に優れており、1ドルあたりのメッシュの成長がmapより悪くないことが条件です。比較レポートは、切り替え（flip）と同時に公開されます。

## 3. 1 回の実行の価値

### 3.1 実行前のスコア予測

未実行の（ペア、モデル、条件）の期待スコアは、意図的にシンプルで完全に検査可能な和です — 双方向の主効果予測に構造化された楽観性を加えたもので、すべての項がアイテムに公開されています。

```
ŝ = clip( pair_prior + model_offset + condition_offset + exploration_bonus,  0, S_CAP )
```

- **`pair_prior`** — 公開済み強度に対する階層的バックオフ: このペアの平均 → このターゲット言語の平均 → このソース言語の平均 → グローバル平均 → `S0_FALLBACK`。使用されたレベルは `prior_basis` として公開されます。
- **`model_offset`** — このモデルが同じペアで*他の*モデルと比較してどの程度優れているか、比較が存在するすべてのペアで平均化したもの。未知のモデルはゼロです。
- **`condition_offset`** — 同じペア（ターゲット言語にフォールバック）での観測済みの coached マイナス naive のデルタ、**それ以外はゼロ**: コーチングゲインは測定された場所では実際のものですが、言語間での転移は仮定されないため、証拠のないペアではベースライン優先の慣例が適用されます。
- **`exploration_bonus`** — 不確実性に対する楽観性、UCB1 スケジュール（Auer, Cesa-Bianchi & Fischer 2002）: `κ·sqrt(2·ln(1+N)/(1+n))`。N は公開済みスコア付き実行の総数、n はこの（ペア、モデル）の実行数です。未試行のセルは最大のボーナスを得ます。十分に測定されたセルはゼロに向かって減衰します。私たちはスケジュール — 未探索のアームが適切なレートで再浮上するような形状 — を借用しており、後悔定理は借用していません。後悔定理はこのシステムが持たない定常バンディットを前提としています。

### 3.2 メッシュゲイン（閉形式）

実行がメッシュを改善できるのは、そのペアのエッジを `s' = max(s(e), ŝ)` に引き上げることによってのみです。単一エッジの変更に対して、任意の 2 言語間の新しい最良チェーンは新しいエッジを無視するか、ちょうど 1 回使用するかのどちらかです。そのため、アップグレードされた行列 — したがって ΔΦ — は正確な 1 行の形式を持ちます（グラフ全体を再解決する必要はありません）。

```
Q'(u,v) = max( Q(u,v),  E(u,a)·s'·E(b,v),  E(u,b)·s'·E(a,v) )

E(x,y) = λ·Q(x,y) for x ≠ y;  E(x,x) = 1        (edge e = {a, b})

ΔΦ = mean over ordered pairs of (Q'(u,v) − Q(u,v))
```

E は「新しいエッジのエンドポイントへの最良チェーンで、それに接続するためのジャンクションを支払うもの」です。2 つの項はエッジを横断する 2 つの方向です。これはハーネススイートで Φ のブルートフォース再計算に対してテストされています。

現在のエッジ強度を上回れない予測は ΔΦ = 0 となります。数式は寄付者のお金を未知の確認ではなく、実証済みの再測定に使わないようにします。（探索ボーナスにより、弱いまたはサンプル不足のセルが永遠に飢えることはありません。）

### 3.3 証拠として何が数えられるか vs. キューに入れられるもの

意図的に非対称な 2 つの異なるゲート:

- **証拠**は*すべての*公開済みかつ失格でない実行から得られます — 公開キューに入れられないコーパス（例: 非商用ライセンスのセット）での実行も含みます。ペアの公開済み測定値は、再実行できるかどうかに関わらず知識です。
- **アクション**（キューアイテム）は、公開実行可能なコーパスのみから得られます: 開発スプリット、CC-BY ファミリーライセンス、誰でも取得可能。

キュー可能でないコーパスを通じてのみ到達可能な言語もグラフに残ります。それらの*周辺*のエッジを改善することでチェーン値が変化し、数式はそれを考慮します。

## 4. パラメータ

| パラメータ | デフォルト | 意味と根拠 |
|---|---|---|
| `λ` (`lambda_junction_discount`) | **0.9** | *推定*チェーンのジャンクションごとの忠実度保持。「直接測定は積が等しいチェーンより優れている」をエンコードします（Utiyama & Isahara 2007; Wu & Wang 2007; Fan et al. 2021）。約 10% のヘアカットはキャリブレーションの選択であり、測定済みチェーントライアングルが蓄積されるにつれて見直されます（§6）。 |
| `κ` (`kappa_exploration_scale`) | **0.05** | 探索ボーナスのスケール（強度単位）。0.05 ≡ 5 chrF++ ポイント — 100 エントリ未満のコーパスではスコア差が区別できないノイズフロア（[コーパス設計 §6.3](/docs/network/specifications/corpus-design)）。楽観性は計測器の分解能で上限が設定されます。 |
| `S_CAP` | **0.95** | 予測上限 — 推定エッジは実証していない完全な忠実度を主張できません。 |
| `S0_FALLBACK` | **0.5** | 最後の手段のペア事前確率。公開済み結果がまったくない場合にのみ使用されます（観測されたグローバル平均 — 最初の 429 回の実行で ≈ 0.54 — は結果が存在する場合は常に優先されます）。 |
| `COST_FLOOR` | **$0.01** | ECV 分母のフロア。ほぼ無料の実行が無制限の 1 ドルあたりの価値を主張できないようにします。 |
| `N_FULL` | **100** | 完全なサイズクレジットのための評価エントリ数（§1.5）。 |
| `L_HEALTHY` | **5.0** | 完全な豊富さクレジットのための有効語数（§1.5）。 |
| `H_NOISE` | **±5 chrF** | 完全な信頼クレジットのための CI 半幅。CI が欠落している場合は 50/√n でプロキシ（n=100 で ±5 に固定）。 |
| `RUNS_FULL` | **2** | 完全なレプリケーションクレジットのための公開済み実行数。 |

**バージョン管理。** パラメータまたは数式の変更は `formula_version`（メタデータ）とこのページのバージョン行を更新します。キューは常に `metadata.priority_parameters` の下で使用された正確な値（現在の Φ を含む）を反映するため、過去のキューは引き続き解釈可能です。感度実行は 1 つのフラグで実行できます: `generate_sweep_queue.py --lam 0.8 --kappa 0.1`。

## 5. 計算例（実際の値、2026-06-12）

424 件のスコア付き実行、59 件の測定済みエッジ、60 言語に対して生成; **Φ = 0.272**。トップアイテム:

```
eng>fao · claude-haiku-4.5 · naive
  edge_strength        0.0      (no published eng→fao runs)
  pair_prior           0.613    basis: target-language (Faroese runs exist via dan→fao)
  model_offset        −0.114    (haiku trails other models on shared pairs)
  condition_offset     0.0      (no coaching evidence for fao)
  exploration_bonus   +0.174    (never-run cell: κ·√(2·ln 425 / 1))
  predicted_strength   0.673
  expected_mesh_gain   0.0181   (eng→fao is a near-component join)
  est_cost_usd         0.0101
  ecv_per_usd          1.79     ← rank #1
```

解釈: フェロー語はデンマーク語を通じてのみメッシュに接続されているため、測定済みの eng→fao エッジは大規模なチェーンファミリーのショートカットとなります（大きな ΔΦ）。このモデルはこのようなペアでは中程度と予測され（事前確率 + オフセット）、このセルは誰も試したことがなく（大きなボーナス）、実行コストは 1 セントです。キューの中で、1 ドルあたりのメッシュ改善がこれを上回るものは他にありません。同じ計算が、すべての入力を公開した状態で、他のすべてのランクを生成します。

## 6. 既知の制限事項（および修正方法）

1. **chrF++ は言語間で比較できません。** 形態論がスケールを変化させます。バスク語への 0.5 エッジはオランダ語への 0.5 エッジと同じ達成ではありません。緩和策: 優先順位はスケール効果が二次的な*構造*（s = 0 → s > 0 の遷移）によって支配されます。修正策: 言語ごとのスコア正規化、またはこれらの言語でより良いクロスリンガルキャリブレーションを持つメトリクスが利用可能になった場合。
2. **積-λ チェーンモデルは事前確率であり、測定値ではありません。** ピボット文献によって方向的に支持されていますが、LLM 翻訳に対してはキャリブレーションされていません。修正策（計画中）: メッシュには現在測定済みのトライアングルが含まれています（例: deu→fra 直接と deu→eng→fra の両方）。そのため、チェーン出力を直接スコアリングし、λ を選択するのではなくデータに適合させることができます。
3. **汚染と開発セットのステータス。** エッジ強度は公開開発セットのすべての注意事項を継承します — Φ を能力の主張としてではなく、上限の計画シグナルとして扱ってください（[コーパス設計](/docs/network/specifications/corpus-design)）。
4. **ドメインの盲点。** 会話テキストで測定されたエッジは 1 つの数値として扱われます。ドメインをまたぐチェーンは λ が予測する以上に劣化します。
5. **方向性。** エッジは現在無向です（X→Y の証拠が X↔Y を点灯させます）。チェーン合成が実際に方向に敏感になった場合、強度は方向ごとに分割されます — 数式は変わらず、グラフが単に 2 倍になります。

## 7. 参考文献

- Latora, V. & Marchiori, M. (2001). *Efficient Behavior of
  Small-World Networks.* Physical Review Letters 87, 198701.
  [arXiv:cond-mat/0101396](https://arxiv.org/abs/cond-mat/0101396)
- Auer, P., Cesa-Bianchi, N. & Fischer, P. (2002). *Finite-time
  Analysis of the Multiarmed Bandit Problem.* Machine Learning 47,
  235–256. [doi:10.1023/A:1013689704352](https://link.springer.com/article/10.1023/A:1013689704352)
- Nemhauser, G., Wolsey, L. & Fisher, M. (1978). *An Analysis of
  Approximations for Maximizing Submodular Set Functions—I.*
  Mathematical Programming 14, 265–294.
  [doi:10.1007/BF01588971](https://link.springer.com/article/10.1007/BF01588971)
- Khuller, S., Moss, A. & Naor, J. (1999). *The Budgeted Maximum
  Coverage Problem.* Information Processing Letters 70(1), 39–45.
  [doi:10.1016/S0020-0190(99)00031-9](https://dl.acm.org/doi/10.1016/S0020-0190(99)00031-9)
- Utiyama, M. & Isahara, H. (2007). *A Comparison of Pivot Methods for
  Phrase-Based Statistical Machine Translation.* HLT-NAACL 2007,
  484–491. [ACL Anthology N07-1061](https://aclanthology.org/N07-1061/)
- Wu, H. & Wang, H. (2007). *Pivot Language Approach for Phrase-Based
  Statistical Machine Translation.* ACL 2007; journal version Machine
  Translation 21(3), 165–181.
  [doi:10.1007/s10590-008-9041-6](https://link.springer.com/article/10.1007/s10590-008-9041-6)
- Paul, M., Yamamoto, H., Sumita, E. & Nakamura, S. (2009). *On the
  Importance of Pivot Language Selection for Statistical Machine
  Translation.* NAACL-HLT 2009 Short Papers, 221–224.
  [ACL Anthology N09-2056](https://aclanthology.org/N09-2056/)
- Haffari, G., Roy, M. & Sarkar, A. (2009). *Active Learning for
  Statistical Phrase-Based Machine Translation.* NAACL-HLT 2009,
  415–423. [ACL Anthology N09-1047](https://aclanthology.org/N09-1047/)
- Fan, A. et al. (2021). *Beyond English-Centric Multilingual Machine
  Translation.* Journal of Machine Learning Research 22(107), 1–48.
  [arXiv:2010.11125](https://arxiv.org/abs/2010.11125)
