---
sidebar_position: 9
title: "عقدة التقييم السيادية — الأجهزة وعمليات العزل المادي (Air-Gap)"
description: "الأجهزة المرجعية، وضوابط العزل المادي (Air-Gap)، وعمليات حفظ المفاتيح لتشغيل عقدة تقييم خاضعة لسيطرة المجتمع: مجموعة الاختبار السرية لا تغادر جهازك أبدًا؛ بل تنتقل الأساليب إلى البيانات."
related:
  - label: "Run a Sovereign Contest"
    to: /docs/network/sovereignty/run-a-sovereign-contest
    kind: doc
    note: "The organizer workflow this node runs"
  - label: "The Derived-Artifacts Commitment"
    to: /docs/network/sovereignty/derived-artifacts
    kind: doc
    note: "Who owns what comes out: you"
  - label: "Benchmark Specification §8 (sandbox)"
    to: /docs/network/specifications/benchmark
    kind: doc
    note: "The isolation model the executor implements"
---

# عقدة التقييم السيادية — الأجهزة وعمليات العزل الشبكي (Air-Gap)

عقدة التقييم السيادية هي جهاز تتحكم **أنت** فيه، ويحتوي على مجموعة اختبار سرية ويُقيّم طرق الترجمة بناءً عليها. تنتقل الطرق إلى البيانات؛ بينما لا تنتقل البيانات على الإطلاق. النتائج — والنتائج فقط — هي ما يخرج من هذه العقدة.

هذه الصفحة هي المواصفات العملية: ما هي الأجهزة التي يجب شراؤها (أو إعادة استخدامها)، وكيفية إعدادها، والانضباط التشغيلي الذي يجعل مقولة "مجموعة الاختبار لم تغادر الجهاز أبدًا" حقيقة يمكنك إثباتها بدلاً من مجرد وعد يجب عليك الوثوق به.

:::info[ما هو متاح اليوم مقابل ما يُصنف قيد التطوير]
برنامج عقدة المنظم (إعداد المسابقة، واستقبال الفرضيات، وتسجيل النقاط المقيّد بالحد الأدنى، ومنفذ الطرق المعزول شبكيًا مع import scan) **ships today** in `mt-eval` — see the [دليل المسابقة السيادية](/docs/network/sovereignty/run-a-sovereign-contest). **مراسم مفتاح الحد الأدنى (threshold key ceremony) وسير عمل البيانات المختومة في وضع السكون (sealed-at-rest) المذكورة في القسم 4 متاحة اليوم أيضًا**: `mt-eval node ceremony init|share|verify|restore`، و`mt-eval node seal`، وحصص النصاب القانوني المقدمة في وقت التشغيل (`node run-method --offline --share …`)، وسجل التفويض المحلي المتسلسل بالتجزئة (hash-chained) (`node ledger verify|head`)، وبيانات النتائج الموقعة (`node sign-manifest` / `node verify-manifest`)، وأدوات العزل الشبكي المذكورة في القسمين 2 و3 (`node bundle`، `node manifest`، `node egress-check`). يظل البديل المتمثل في زوج مفاتيح واحد متاحًا فقط للمسابقات التي يمتلك فيها المنظم المراجع بالكامل — وتوضح كل واجهة أي مسار قيد الاستخدام. بعبارة واضحة، ما **لا** يتضمنه الإصدار الأول (v1): لا يُدعى وجود مصادقة عن بُعد للأجهزة (TEE) (القسم 5)، كما أن *التوقيع* بالحد الأدنى من جانب المنصة (موافقات هاتف الوصي مقابل البنية التحتية المستضافة) هو عمل مستقبلي — في العقدة السيادية، تُمارس الوصاية من خلال التقديم الفعلي لـ M من N حصة في الجهاز (القسم 4). ولنكون دقيقين بشأن التشفير: هذه هي مشاركة سر شامير (Shamir M-of-N secret sharing) حيث يتم **إعادة بناء المفتاح في الذاكرة المقفلة للعقدة أثناء التشغيل المصرح به** (ثم تصفيره) — إنها *ليست* حوسبة متعددة الأطراف (multi-party computation)، والمفتاح يتواجد مجمّعًا لفترة وجيزة على جهازك غير المتصل بالإنترنت. أخيرًا، حتى تُفتح بوابة الموافقة المجتمعية، يعمل المسار على **البيانات الاصطناعية فقط**؛ وتنتظر المجموعات النصية الحقيقية تلك الموافقة.
:::

## 1. الأجهزة المرجعية

يُشغّل المنفذ طرقًا مستقلة بذاتها: فك تشفير الترجمة الآلية العصبية (NMT) محليًا، والتحقق من صحة محولات الحالة المحدودة (FST)/علم الصرف، وحساب المقاييس. لا تحدث أي استدعاءات سحابية داخل العزل الشبكي (طرق واجهة برمجة تطبيقات النماذج اللغوية الكبيرة (LLM-API) هي بالضبط الفئة التي ترفضها العقدة المعزولة شبكيًا — راجع فئات الطرق في [مواصفات المعيار](/docs/network/specifications/benchmark)).

| الفئة | المواصفات | الملاءمة | التكلفة التقريبية (2026) |
|---|---|---|---|
| **الحد الأدنى** (يعمل) | معالج 4 أنوية x86_64 أو Apple/ARM، ذاكرة وصول عشوائي (RAM) 16 جيجابايت، قرص SSD بسعة 500 جيجابايت | تقييم المقاييس + FST، فك تشفير وحدة المعالجة المركزية (CPU) لنماذج NMT الصغيرة (بطيء ولكنه صحيح) | 0 دولار أمريكي (حاسوب محمول احتياطي) – 400 دولار أمريكي (مستعمل) |
| **الموصى به** | معالج 8 أنوية، ذاكرة وصول عشوائي (RAM) 32 جيجابايت، قرص NVMe بسعة 1 تيرابايت، وحدة معالجة رسومات NVIDIA بذاكرة VRAM ≥ 12 جيجابايت (مثل فئة RTX 4070) | فك تشفير NMT مريح لمجموعات الاختبار الكاملة؛ تقييم الطرق المتوازية | ~900–1,600 دولار أمريكي (محطة عمل صغيرة الحجم) |
| **المؤسسي** | معالج 16 نواة، ذاكرة وصول عشوائي (RAM) 64–128 جيجابايت، قرص NVMe بسعة 2 تيرابايت، ذاكرة VRAM بسعة 24 جيجابايت فأكثر | مسابقات متعددة الطرق، مجموعات اختبار كبيرة، مخزن نصوص مشفرة مؤرشفة | ~2,500–4,000 دولار أمريكي |

المتطلبات الأساسية في كل فئة:

- **عدم وجود أجهزة اتصال لاسلكي (Radios)، أو أجهزة يمكنك إثبات إيقاف تشغيلها.** الأفضل: حاسوب مكتبي بدون بطاقة Wi-Fi/Bluetooth. المقبول: حاسوب محمول تمت إزالة بطاقته اللاسلكية فعليًا أو تعطيلها في البرامج الثابتة (firmware). "وضع الطيران" ليس عزلاً شبكيًا.
- **بطاقة شبكة سلكية (NIC) يمكنك تركها غير متصلة.** غياب الكابل هو أكثر أدوات التحكم في الشبكة قابلية للتدقيق.
- **محرّكا أقراص USB مخصصان** (يحملان علامتي IN و OUT — راجع القسم 3)، ومن الناحية المثالية، جهاز تقوم بتعطيل منافذه الأخرى في البرامج الثابتة.
- **تشفير كامل للقرص** (LUKS على نظام Linux) بحيث تصبح العقدة المسروقة عديمة الفائدة، ومزود طاقة غير منقطعة (UPS) إذا كان مصدر الطاقة لديك غير موثوق — يمكن استرداد التقييم الذي تمت مقاطعته في منتصف مجموعة الاختبار، ولكن لماذا تخاطر باكتشاف ذلك.

## 2. إعداد البرامج (لمرة واحدة، ~ساعة واحدة)

1. قم بتثبيت إصدار Linux LTS حالي (Ubuntu/Debian) من مثبت USB **مع فصل كابل الشبكة**؛ وقم بتمكين التشفير الكامل للقرص عند التثبيت.
2. على جهاز منفصل متصل بالإنترنت، قم ببناء الحزمة غير المتصلة بالإنترنت — `mt-eval node bundle --out <dir>` حزم wheels `mt-eval[node]` وتبعياتها، ونسخ أي عناصر `--include`، وكتابة قائمة sha256 لكل ملف. كل ما تحتاجه العقدة يعبر عبر محرك الأقراص IN مرة واحدة.
3. انقل الحزمة على محرك الأقراص IN؛ وتحقق من sha256 لكل عنصر مقابل القائمة **على العقدة** قبل التثبيت (`mt-eval node bundle --verify <dir>`).
4. قم بإنشاء زوج مفاتيح التوقيع الخاص بالعقدة (`mt-eval node keygen`) وسجل النصف العام منه — ستقوم بنشره حتى يتمكن أي شخص من التحقق من قوائم النتائج الخاصة بك (القسم 5).
5. من تلك اللحظة فصاعدًا، لن يتصل الجهاز بأي شبكة أبدًا — ويمكن إجراء تشغيل مختوم لإثبات ذلك أولاً: `mt-eval node egress-check` (يُفرض أيضًا تلقائيًا باستخدام `assert_airgap` في تكوين العقدة) يرفض التشغيل عندما يُظهر أي مسار أو فحص أو DNS أي طريق للاتصال الخارجي. تحديثات نظام التشغيل هي حدث متعمد ومُجمّع ومُتحقق منه بالتجزئة — وليست خدمة تعمل في الخلفية.

## 3. انضباط النقل (كل مسابقة، في كلا الاتجاهين)

العزل الشبكي هو *إجراء*، وليس منتجًا. الإجراء هو:

- **محرك الأقراص IN** يحمل: حزم الطرق المُقدمة، وملفات الفرضيات، وقائمتها. قبل تشغيل أي شيء، تتحقق العقدة من تجزئة كل حزمة مقابل القائمة ويتم تشغيل فحص الاستيراد (ترفض الطرق التي تستورد مكتبات الشبكة — هذا متاح اليوم).
- **محرك الأقراص OUT** يحمل: قائمة النتائج الموقعة — النتائج الإجمالية، وتجزئات الطرق/التكوينات التي تنتمي إليها، ورأس سجل التدقيق — و*لا شيء غير ذلك*. تظل المخرجات الخاصة بكل مقطع على العقدة تحت سيطرة المنظم؛ ونشرها هو قرار مجتمعي منفصل ومتعمد.
- اتجاه واحد لكل محرك أقراص، دائمًا. محرك الأقراص الذي لامس العقدة لا يتم تركيبه تلقائيًا (auto-mount) أبدًا على جهاز متصل بالإنترنت — قم بتركيبه `noexec,nodev` وانسخ القائمة يدويًا.
- `mt-eval node manifest write <drive> --direction in|out` يقوم بتجزئة كل ملف على محرك الأقراص قبل العبور؛ `mt-eval node manifest verify` على الجانب المتلقي يرفض أي شيء تمت إضافته أو تغييره أو فقدانه.
- سجل كل عبور (التاريخ، محرك الأقراص، تجزئة القائمة) في السجل الورقي للعقدة أو السجل الموجود على العقدة. الروتين هو الهدف: السجل هو ما يتيح لك الإجابة على سؤال "هل غادر أي شيء آخر على الإطلاق؟" بالأدلة.

## 4. وصاية المفتاح (M من N، محتفظ بها مجتمعيًا)

مجموعة الاختبار المختومة مشفرة في وضع السكون؛ ويتطلب فك التشفير نصابًا قانونيًا من حصص المفاتيح التي يحتفظ بها أوصياء **يختارهم المجتمع** — مجلس شيوخ، أو هيئة لغوية، أو هيئة تعليمية. لا تحتفظ المنصة بأي حصص؛ ولا يمكن لـ Champollion فك تشفير مجموعة مختومة، ولا يمكن لأي وصي بمفرده القيام بذلك.

المراسم (جلسة واحدة غير متصلة بالإنترنت؛ الأدوات المتاحة تقوم بأتمتتها): `mt-eval node ceremony init` يُنشئ مفتاح المجموعة على العقدة، ويقسمه إلى N حصة (أي M منها تعيد بناءه؛ وأقل من ذلك لا يكشف شيئًا — المشاركة مبنية على نظرية المعلومات)، ويُصفّر المفتاح في نفس اللحظة؛ `ceremony share` يُصدر حصة كل وصي كملف لرمز مميز (token) بالإضافة إلى نسخة احتياطية ورقية قابلة للطباعة؛ `ceremony verify` يُثبت أن النسخ الموزعة قادرة على إعادة البناء — دون حفظ أي شيء؛ `ceremony share --wipe-originals` then destroys the node's own copies. `mt-eval node seal` يُشفر المجموعة النصية باستخدام المفتاح العام للمراسم: تُخزن العقدة النص المشفر وبطاقة بيانات وصفية خالية من المحتوى، ولا شيء غير ذلك. من تلك اللحظة فصاعدًا، يعني تشغيل التقييم أن الأوصياء يقدمون فعليًا M من N حصة (`node run-method --offline --share …`): يتم إعادة بناء المفتاح **في الذاكرة المقفلة للمنفذ فقط**، ويُستخدم لهذا التشغيل الواحد المرتبط بالمنحة، ثم يُصفّر — ولا يلامس القرص مرة أخرى أبدًا. يُضاف كل طلب وتصويت ومنحة واستخدام إلى سجل محلي متسلسل بالتجزئة (`node ledger verify`)، وتُرفض أي محاولة بدون نصاب قانوني *و*تُسجل.

جملة صادقة واحدة حول الآلية: هذه هي مشاركة سر شامير (Shamir secret sharing) مع إعادة البناء في ذاكرة الجهاز غير المتصل بالإنترنت الذي يحتفظ به المجتمع — وليست حوسبة متعددة الأطراف. أثناء التشغيل المصرح به، يتواجد المفتاح لفترة وجيزة، مُجمّعًا، على أجهزة يتحكم فيها المجتمع فعليًا؛ الخصائص التي يدافع عنها هي *عدم وجود مفتاح دائم على القرص*، و*عدم وجود تشغيل بدون حضور النصاب القانوني*، و*كل استخدام متسلسل في السجل القابل للفحص*. التوقيع بالحد الأدنى من جانب المنصة، حيث لا يتم تجميع المفتاح في أي مكان أبدًا، يظل عملاً مستقبليًا ويُصنف على هذا النحو أينما ذُكر.

يؤدي التدوير واستبدال الأوصياء إلى إعادة تشغيل المراسم؛ ويعني فقدان أكثر من N−M حصة إعادة ختم المجموعة من النسخة المصدرية للمجتمع — يحتفظ المجتمع دائمًا بنسخته الأصلية ذات النص العادي، لأن [الحيازة](/docs/network/sovereignty/data-sovereignty) لم تكن أبدًا من حقنا.

## 5. ماذا تعني كلمة "مُصادق عليه" (attested) هنا — وماذا لا تعني

يُنتج كل تقييم **قائمة نتائج موقعة**: توقيع العقدة على النتائج، وتجزئات حزمة الطريقة، والمجموع الاختباري (checksum) للمجموعة النصية، ورأس سجل التدقيق المخصص للإلحاق فقط. يمكن لأي شخص يحمل المفتاح العام المنشور للعقدة التحقق من ذلك — `mt-eval node verify-manifest <manifest> --pubkey <published .pub.json>` — أن *هذه العقدة* أنتجت *هذه النتائج* لـ *هذه المدخلات الدقيقة*، ويجعل السجل المتسلسل بالتجزئة التعديلات الصامتة على السجل قابلة للاكتشاف.

هذه هي **المصادقة البرمجية** — فهي تثبت سلامة السجل، وهذا ما يقدمه الإصدار الأول (v1). إنها **لا** تثبت أي شريحة سيليكون نفذت التشغيل: المصادقة عن بُعد للأجهزة (TEEs) هي عمل مستقبلي ولا يُدعى وجودها عمدًا. البيان الأمني الصادق للإصدار الأول (v1): انضباط المنظم (القسم 3) بالإضافة إلى القوائم الموقعة بالإضافة إلى الوصاية المادية للمجتمع على الجهاز هو مرساة الثقة — وهو بالضبط المكان الذي يريد التصميم القائم على السيادة أولاً أن تستقر فيه الثقة على أي حال.

## 6. حلقة التشغيل

1. أعلن عن المسابقة؛ وانشر المفتاح العام للعقدة + الحد الأدنى لمجموعة التطوير (dev-set threshold).
2. استلم المشاركات عبر الإنترنت (جهاز عادي)، وقم بتجميع قائمة IN (`mt-eval node manifest write <drive> --direction in`).
3. احمل محرك الأقراص IN إلى العقدة؛ وتحقق من التجزئات (`node manifest verify`)؛
   import-scan (`node import-bundle`); queue methods.
4. يُصرح الأوصياء بالتشغيل من خلال تقديم نصاب قانوني من الحصص (القسم 4 — `node run-method <id> --offline --share … --share …`)؛ يتم فك تشفير المجموعة المختومة في المنفذ فقط. لا يوجد نصاب قانوني، لا يوجد تشغيل — وتُسجل المحاولة في السجل.
5. التنفيذ؛ تُحسب النتائج؛ يُحتفظ بالمخرجات الخاصة بكل مقطع على جانب العقدة.
6. التفكيك (Teardown): يُمسح النص العادي العامل؛ يُلحق سجل التدقيق؛ تُوقع القائمة.
7. احمل محرك الأقراص OUT مرة أخرى؛ وانشر النتائج + القائمة؛ يمكن لأي شخص التحقق (`node verify-manifest`).
8. سجل العبور؛ تظل محركات الأقراص مخصصة؛ وتظل العقدة غير متصلة (dark).

